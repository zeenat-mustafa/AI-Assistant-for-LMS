# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12

# LLM providers
google-generativeai==0.8.3
groq>=0.13.0

# Notebook handling
nbformat==5.10.4

# Session matching
rapidfuzz==3.10.1

# MCP
# 2.x is the current stable line (1.x is maintenance-only: critical fixes and
# security patches only). Chosen in Phase 4.1 while there was no MCP code to
# migrate, so the switching cost was zero. Note 2.x renamed FastMCP ->
# MCPServer; code written against 1.x tutorials will not run as-is.
mcp==2.1.1

# Utilities
python-dotenv==1.0.1
pydantic-settings==2.6.1
# Bumped from 2.10.6 in Phase 4.1: mcp 2.x requires pydantic>=2.12.0, so the
# old pin would fail a clean `pip install -r requirements.txt`. Full suite
# re-run on the new version with zero regressions.
pydantic[email]==2.13.5
aiofiles==24.1.0
