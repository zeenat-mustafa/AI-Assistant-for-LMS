# AI Assistant for LMS — Project Memory

## What this is
Capstone project (Purelogics Bootcamp): instructor-directed AI grading assistant for Jupyter notebooks. Instructors create Sessions ("Week X Day Y"), upload unsolved assignments, students download/solve/upload back, instructor triggers grading via chat instruction. Standalone app, no real LMS integration. Team: Zeenat Mustafa + Laiba Afreen. Repo: https://github.com/zeenat-mustafa/AI-Assistant-for-LMS (public).

## Working method (always follow this)
When asked for the next feature/sub-feature prompt, always give a FULLY DETAILED, A-to-Z prompt: exact files to create/edit, exact function names, exact internal logic step-by-step, exact testing requirements, and explicit scope boundaries (a "Do NOT" list). Never leave architecture/library/approach decisions to guesswork. For any resume/handoff situation, verify actual current file contents against a checklist before continuing — never trust a prior summary alone.

## Tech stack
FastAPI (Python) + SQLite/SQLAlchemy + Alembic migrations + JWT/bcrypt auth + local filesystem storage. AI: two-tier Gemini only — primary `gemini-3.5-flash-lite`, fallback `gemini-3.1-flash-lite` on quota/rate-limit — behind one shared `app/services/llm_provider.py` (Groq and Ollama removed; no fast/pro split — one primary/fallback pair for every call). Conda env: `base` (not `ai-bootcamp` — confirmed via pip show). Next.js 16.3.4 frontend in `frontend/` — App Router, TypeScript, Tailwind 4, Vitest; typed API client in `src/lib/api/` (Phase 5, complete). MCP server: `mcp` SDK 2.x, stdio transport, `app/mcp/` (Phase 4, complete). RAG (Phase 7): `python-pptx` lecture extraction, local `sentence-transformers` (`all-MiniLM-L6-v2`) embeddings, embedded file-based Chroma vector store. Alembic/pytest must run with the conda base Python (`C:\ProgramData\miniconda3\python.exe`) — the Windows default `python` (3.14) has no alembic.

## Core design decisions (locked in — apply to every future feature)
- Sessions have 3 structural areas: Assignment Files, Submissions, Grades & Feedback — file role never guessed from filename.
- No instructor rubric — AI generates its own per unsolved file, worth 10 marks, cached & reused per student. Completion-aware: scaffolding capped ~1-2 marks, majority of marks on real student work. Completion points can be TODO keywords, informal comments, OR markdown-only, OR code-comment-only (no markdown at all) — all supported styles.
- File/session matching is content-based first, filename-based fallback, confidence-threshold gated (0.55) — never force-match on ambiguity, always leave for instructor review instead of guessing.
- Scoring: 0.5-point increments only, everywhere. Multi-file Session combined score = sum of per-file scores / TOTAL assignment count in the session (not just files the student submitted) — missing/unmatched/ungraded files count as 0 in the denominator, for fairness across students.
- Every grade stores full criterion-by-criterion rationale (`rationale_json`) for the future "why this grade" student chatbot (Extended Goal).
- Grading only happens on explicit instructor trigger, never automatic.
- Any schema change MUST go through an Alembic migration (`alembic revision --autogenerate`) — never rely on `create_all()` for an existing non-empty DB; it only creates missing tables, never alters existing ones. `create_all()` is still fine for a brand-new empty dev DB.
- Evaluator must verify claimed execution against real `execution_count`/output evidence — never credit a criterion claiming a cell "ran/executed/produced X" without real evidence.
- Evaluator must penalize substituting a custom implementation for an explicitly NAMED required tool/library (not just a goal/outcome) — cap at ~20-30% of that criterion's points, excluding failed/abandoned install attempts from counting as partial use.
- Instructor access is a shared faculty workspace, not per-instructor isolation (decided 2026-09-10): every instructor sees/reads/edits/grades every session regardless of who created it. `instructor_id` stays on the data model purely for "created by" display, never for gating access. Student-facing access control is unaffected — students still only see their own submissions/grades. Full details: `docs/phase5-known-gaps-record.txt`.

## Current status (update this section as phases complete)
- **Phase 1 (Foundation): COMPLETE.** Auth, session CRUD, assignment/submission upload, grade-report endpoints.
- **Phase 2 (AI Grading Pipeline, sub-features 2.1-2.9): COMPLETE, merged to `main`.** Full real-data validation done (not just mocked tests) — 3 genuine grading-quality bugs found and fixed (file-matching exclusion of code-comment-only assignments, execution-hallucination in evaluator, named-tool-substitution under-penalization), plus a live-DB schema gap resolved via Alembic. 223 tests passing. Full details: `Phase2_Completion_Record.pdf` (kept outside repo) and `docs/phase2-known-gaps-record.txt`.
- **Phase 3 (Instructor Chatbot + Session Matching, sub-features 3.1-3.5): COMPLETE, merged to `main`.** 3.1 (session matching) → 3.2 (filter parsing) → 3.3 (`POST /chat`) → 3.4 (`POST /chat/stream`, SSE) → 3.5 (conversational summary messages); 3.6 (never guess on ambiguity) was folded into 3.1 as planned, not built separately. Required one prereq schema change first: `LMSSession` had no instructor ownership at all, so `instructor_id` + per-instructor title uniqueness were added via Alembic migration `b824df1246e5`. Full real-data validation done (not just mocked tests) — 8 genuine bugs found and fixed, 5 of them only surfacing through real Swagger/dev-DB testing (single-item "ambiguous" candidate list, swapped week/day numbers tying at confidence 1.0, digit near-misses still fuzzy-scored, `combined_score` rounding to 0.01 instead of 0.5, and an incomplete dev-DB reset that left 25 orphaned files in `storage/sessions/`). SSE streaming was verified live with timestamped `curl -N` output, not assumed. 273 tests passing. Full details: `docs/phase3-known-gaps-record.txt`.
- **Phase 4 (MCP Server, sub-features 4.1-4.6): COMPLETE, merged to `main`.** 4.1 (scaffold + `ping`) → 4.2 (`match_session`) → 4.3 (`generate_rubric`) → 4.4 (`evaluate_submission`) → 4.5 (`grade_submission_file`, `grade_session`) → 4.6 (chained-workflow verification). Six tools total, all thin wrappers in `app/mcp/tools/` (one module per domain, each exposing `register(server)`; `server.py` stays a thin registry). Every tool opens its own `SessionLocal`, calls the existing Phase 1-3 service directly, and returns its result **unaltered** — no logic is reimplemented, and parity tests guard MCP-vs-REST output so the two interfaces can't drift. Required upgrading the pinned MCP SDK from a 20-month-stale 1.2.0 to 2.1.1 (renames `FastMCP`→`MCPServer`; forced pydantic 2.10.6→2.13.5, re-verified with zero regressions). Transport is stdio; `grade_session` deliberately drains the generator since MCP calls are request/response — live progress stays with `/chat/stream` (3.4). 313 tests passing. Real verification throughout (real subprocess + real MCP client + real dev DB), ~10 Gemini calls across the whole phase. Full details: `docs/phase4-known-gaps-record.txt` — note especially that there are NO discovery tools, so three of the six tools need ids an MCP client cannot obtain through MCP (deferred by decision; revisit before Phase 6).
- **Phase 5 (Next.js Frontend, sub-features 5.1-5.6): COMPLETE, merged to `main`.** 5.1 (scaffold + typed API client) → 5.2 (auth pages + role-based protected routing) → 5.3 (instructor sessions + assignment files) → 5.4 (grades roster + embedded grading chat) → 5.5 (student session list + downloads) → 5.6 (student upload + own grades). Lives in `frontend/` — Next.js 16.3.4 (App Router, TypeScript, Tailwind 4), with `src/lib/api/` mirroring every backend router one module per file; nothing outside that folder calls `fetch` against the backend. **Zero backend files were modified in the entire phase.** Tests are a SEPARATE Vitest suite: **141 frontend tests** alongside the unchanged **313 backend pytest tests** — 141 is not a continuation of 313, the two run independently (`npm test` in `frontend/`, `pytest` in `backend/`). Real Gemini spend for the whole phase was **2 grading calls**. Four findings worth carrying forward: an unanchored `lib/` rule in the root `.gitignore` was silently excluding the entire API client from git (fixed with `!frontend/src/lib/`); **re-uploading a submission replaces it and destroys its grades** via cascade, which 5.6 handles with a warn-and-confirm naming the real score (a UX guardrail only — the endpoint is unprotected for any other caller); `combined_score` is `0.0` for BOTH a genuine zero and an ungraded submitter, so grading status must always be read from the per-file `graded` flag or `per_file` presence, never from the score; and `/chat/stream` has no `session_id` field at all, so the chat panel is scoped by prefilling the instruction text, not by payload. Full details: `Documentation/Phase5_Completion_Record.pdf` (kept outside repo) and `docs/phase5-known-gaps-record.txt` (23 gaps).
- **Post-Phase-5 bug fixes (backend + frontend): backend on `main`, frontend PR pending merge.** Two companion branches, four commits each, one fix per commit, every fix verified live against the real backend + dev DB (not only mocked).
  - **Backend (`bugfix-post-phase5`, merged to `main`):** Fix 1 — a nonexistent week/day number must never fuzzy-match a filler word in an instruction. Fix 2 — non-notebook assignment files now live in a **separate `resource_files` table** (a new `ResourceFile` model, NOT a role column on `UnsolvedFile`), added via Alembic migration `4a13a49d0ad4`: `SessionRead` gains a `resource_files` field alongside the unchanged notebook-only `unsolved_files`; new `GET .../assignments/resources` (list) and `GET .../assignments/resources/{id}/download` endpoints; the upload response derives `file_role` (`notebook`|`resource`) from which table a row landed in — it is response metadata, never a stored column. A `.zip` with only resources, or a mix, now uploads successfully; the duplicate guard spans BOTH tables so a batch stays atomic; there is deliberately **no delete-resource endpoint**. Fix 3 — when all LLM providers fail, the API returns a clean generic message, never the raw multi-provider dump (`grading_pipeline._user_facing_error`, keyed on a stable signature substring). Fix 4 — a non-grading question returns an honest `unrecognized_instruction` message instead of falling through to "no such student". Backend suite grew 313 → **333 tests**. Details: `docs/phase5-known-gaps-record.txt` plus the four commit messages.
  - **Frontend (`bugfix-post-phase5-frontend`, pushed; PR into `main` open/pending):** Fix A — instructor upload UI shows two separate lists (**Notebooks** gradeable / **Resource files** not-graded) from the two separate backend fields, no Remove control for resources (no endpoint exists); `uploadAssignment` retyped to return `AssignmentUploadItem[]` (was mistyped as `UnsolvedFileRead[]`); download busy-state keyed by role+id because notebook and resource ids genuinely collide (both tables start at 1); roster denominator stays notebooks-only. Fix B — students see and download the same two lists (resources routes are `get_current_user`, not instructor-only). Fix C — chat renders the backend's clean message verbatim, with a defensive `lib/chat-safety.ts` backstop that swaps in a generic line + console-logs the real text only on raw markers (`quota_metric`/`org_id`/`WinError`) or absurd length (>600 chars; longest real message is 157). Fix D — per-file grade detail (the Phase 2 formatted per-criterion feedback text, not the still-unrendered structured `rationale`) collapses behind an in-place toggle on BOTH the student panel and instructor roster via a shared `components/grade-file-row.tsx`; combined score stays the headline; zero-vs-ungraded distinction preserved; two existing tests that asserted `toBeInTheDocument` (passes for hidden elements) rewritten to assert visibility. Frontend suite grew 141 → **184 tests**. New shared helpers: `lib/download.ts`, `lib/chat-safety.ts`, `components/grade-file-row.tsx`, `components/ui.tsx`'s `FileRoleBadge`. One infra fix: `.claude/launch.json` now starts uvicorn from `backend/` — the CWD-relative `sqlite:///./lms.db` otherwise silently creates a second, empty DB at the repo root.
- **Phase 6:** Not started. Integration testing / polish / demo prep.
- **Phase 7 (RAG + Memory-Aware Student Chatbot + In-Chat Quizzes + UI Overhaul, sub-features 7.1-7.8): IN PROGRESS on `phase-7` — 7.1-7.6 COMPLETE (committed); not yet merged to `main`** (merge waits for full-phase completion or a deliberate handoff, per Phases 2-5 precedent). Phase 7 supersedes the "Extended Goals" and the "Full RAG pipeline" Future Work item below where they conflict: full RAG is now actively being built, and 7.6's quiz is a student-triggered, non-grade-affecting practice quiz — NOT the original instructor-published graded quiz (never conflate the two). Full plan + standing rules: `phase7-full-plan.md` (kept outside repo). **LLM provider chain correction:** the chain is TWO Gemini tiers only (primary `gemini-3.5-flash-lite` → fallback `gemini-3.1-flash-lite` on quota/rate-limit) behind `llm_provider.py` — the Phase 7 planning docs and sub-feature prompts still say "Gemini → Groq → Ollama" (e.g. 7.6's generation flow); that is outdated, Groq/Ollama were removed long before Phase 7 — never reintroduce them or plan around a third tier. Backend suite 333 → **472 tests** across 7.1-7.4, → **474** after the lifespan-isolation fix, → **545** after 7.5, → **664** after 7.6 (634 before its pre-commit fixes), all passing.
  - **7.1 (lecture `.pptx` ingestion):** new `LectureFile`/`LectureChunk` models + migration `e15a0ce634e1`, `lecture_extraction` service, `lectures` router (instructor-only upload; any authenticated user can list/download, mirroring `ResourceFile`). `.pptx` only — `.ppt` rejected outright. `python-pptx` extracts slide text, speaker notes, and table shapes (checked on every shape, labeled `[Table]`). Chunks are 800 chars / 150 overlap and never cross a slide boundary or mix slide text with notes, so citations stay precise. Raw bytes preserved (sha256-verified download integrity). No embeddings in 7.1.
  - **7.2 (Chroma + embeddings):** local `sentence-transformers` `all-MiniLM-L6-v2` (never Gemini's embedding API — RAG call volume is far higher than grading) into ONE global Chroma collection metadata-filtered by `session_id`. Embeds every `LectureChunk` and every **unsolved** notebook's markdown/code cells — never `Submission`/`SubmissionFile` content, by construction. `embedded`/`embedding_error` tracking on both tables via migration `b0bae8c363e6`; synchronous embedding wired into both upload flows + a one-time backfill (23/23 chunks, 22/22 notebooks / 856 cells, 0 failures). `retrieve()` never force-returns weak matches below the empirically tuned 0.35 similarity threshold.
  - **7.3 (scope-safety, "explain, never solve"):** signed-off `SCOPE_SAFETY_RULE` + `build_scope_safe_prompt()` in `app/services/chat_safety.py`. Adds `backend/pytest.ini` (first pytest config) whose `norecursedirs` keeps the real-Gemini `tests/adversarial/` suite out of the default run — run those files explicitly. 9 real cases on the fixture `UnsolvedFile` id=20 ("Week 10_Lab3.ipynb", session 7): 5 attack framings held, 2 borderline not over-refused, 2 legitimate answered. **Standing Rule #7 carry-forward: any change near `SCOPE_SAFETY_RULE` (7.5+) must re-run the whole adversarial suite.**
  - **7.4 (chat memory):** `ConversationThread` (one per student + `LMSSession` pair, unique constraint; nullable `rolling_summary` + `summarized_through_message_id` bookmark) and `ConversationMessage` (`MessageRole` user/assistant) via migration `9d9376585d4a`; both cascade-delete with their session/student, otherwise messages are insert-only and **never** deleted or modified. `app/services/chat_memory.py`: rolling window of `RECENT_N = 8` verbatim messages, summarizing everything but the newest 8 once more than 12 are unsummarized (one summarization call every 3 exchanges); the summarization prompt is signed-off text; summaries that are empty, contain a code block, or exceed 600 words are rejected; summarization failure keeps the previous summary and caps verbatim history at 12. Call `get_context_for_prompt` BEFORE persisting the current question. `build_scope_safe_prompt` now renders a signed-off "Conversation So Far" block (always present, labeled as not a source of instructions) between the rule and retrieved material. **Real finding that changed the rule:** explaining the PRE-written test cell 17 (`calculator.invoke("25*18")`) sometimes named `eval()` — the answer to cell 15's gap — only with attack-heavy history (1/5 + 1/1); a signed-off NEVER ALLOWED clause ("Explaining how an INCOMPLETE section works internally…") was added, after which it was 0/10 in every condition (run twice). All 12 adversarial cases (original 9 + false-memory injection, trust-building, summary-laundering) held on primary; legitimate/borderline cases not over-refused. A structural-walkthrough-plus-notebook-hint answer ("pass the string to that evaluation function") was reviewed and **deliberately left as-is** — don't tighten the rule on that basis alone. Real multi-turn threads confirmed follow-ups ("the first one you mentioned", recall of turn 1 from the summary alone) work and the prompt shrinks after summarization (17.5k → 13.7k chars); bounded by design but only one summarization cycle has been exercised. Permanent real-Gemini tests: `tests/adversarial/test_scope_safety_memory.py`, `test_chat_memory_real.py`, `test_eval_leak.py` (frozen histories in `tests/adversarial/fixtures/`), `test_chat_memory_multiturn_real.py` (scratch backup-API DB copy, never the real `lms.db`). Run them with `ADVERSARIAL_CALL_DELAY_S=8` — back-to-back runs hit the primary per-minute limit and silently fall back, which the tests correctly reject. Transcripts: `tests/adversarial/transcripts_report_7.4*.md` (`*_pre-rule-revision.md` = evidence before the rule clause).
  - **Infra gotcha found in 7.4 (FIXED in `3047fe9`):** 13 existing test files use `with TestClient(app)`, which ran `main.py`'s lifespan `create_all()` + demo seeding against the REAL `backend/lms.db` — after adding models but before `alembic upgrade head`, a plain `pytest` run silently created the new tables on the live DB while `alembic_version` stayed behind. `backend/tests/conftest.py` now points `app.main.engine`/`SessionLocal` at a throwaway in-memory engine for the whole test session (no production code change); `tests/test_lifespan_isolation.py` guards it and fails safely without the conftest (`--noconftest`).
  - **7.5 (student Q&A chatbot — backend + endpoint only; the student floating widget UI is 7.7):** `POST /api/v1/student-chat/stream` (new router `app/routers/student_chat.py`, `require_student` only). Brings 7.1-7.4 together: resolve session → that session's 7.4 thread → scoped retrieval (top-5, 0.35) → UNCHANGED `build_scope_safe_prompt` (no new framing added near `SCOPE_SAFETY_RULE`) → `call_llm_stream`. SSE (same `data: {json}\n\n` framing as `/chat/stream`): `resolved` → `citations` (the chunks GIVEN to the model: filename, slide/source or cell index/type, session, similarity, 200-char snippet — not proof of what the answer used) → `token`… → `done`; or a single `clarification_needed` (message + ≤3 candidates) and close; or `error` with a clean generic message (never raw provider text). The question + answer are persisted together ONLY after the stream completes — a model failure or client disconnect stores nothing, so a thread never holds a question without its answer; no thread is created on the clarification path. **Frontend contract (for 7.7):** send `current_session_id` = the open page's session, or else the `session_id` the previous turn's `resolved` event returned (that is how follow-ups keep their thread); to answer a clarification, re-send the original question with the chosen candidate as `current_session_id`.
    - **Streaming provider:** `llm_provider.call_llm_stream` added alongside the untouched `call_llm`. Fallback to the second Gemini tier is possible ONLY before the first chunk (streamed text can't be retracted); a mid-stream failure raises `LLMProviderError`. It logs the same "LLM call served by gemini primary/fallback" line on completion, so existing tier checks work. Verified live: 11 real chunks over ~0.8s; over real HTTP, tokens arrive every ~50-140ms.
    - **Session resolution (`app/services/chat_session_resolver.py`, signed off):** with page context C, stay in C unless another session is CLEARLY better (its best broad similarity ≥ 0.55 AND beats C's best scoped similarity by ≥ 0.15) → `redirected`; weak/zero retrieval stays in C so follow-ups work. Without context, resolve only on a clear winner (best ≥ 0.55 AND leads the runner-up session by ≥ 0.10 OR holds ≥ 70% of the top-10 hits) → `broad_search`; otherwise ask. **Vague-reference guard (Step 6 finding, option (b)):** without context, a question referring to an unnamed "this assignment/lab/notebook/homework/exercise/task/week/session" ALWAYS asks — "What should I do for the TODO in this assignment?" (0.5376, asked) vs "what do I do for the TODO in this assignment" (0.5664, was silently resolved) proved no similarity threshold can separate vague from specific; "this code" is deliberately excluded (students paste the code). Final thresholds unchanged from the starting values. Never raises; never fabricates a match.
    - **Real-data facts worth knowing:** the same `Week10_Day2.ipynb` is uploaded to sessions 5, 6 and 7, so some questions (e.g. `bind_tools`) legitimately ask for clarification between them. The FIRST request after a server start takes ~20+s before its first event while the sentence-transformers model loads (one-time per process; not addressed).
    - **Verification (all real primary Gemini):** the full adversarial suite re-run THROUGH the real endpoint — 7.3/7.4's 12 cases + 2 no-context attacks held; `eval()` leak 0/10 in all 3 conditions (attack history replayed byte-identically as real stored messages); real multi-turn threads (first turn broad-search, later turns echo the resolved session) kept continuity, recalled from the summary, summarized only at turn 8. Step 6 on a live uvicorn server (student2/student3 demo accounts) covered all 4 branches, all 26 citations checked against real notebook cells/lecture chunks, both borderline phrasings re-verified asking after the guard. Closest calls, same accepted borderline class as 7.4 (left as-is): naming "the weather tool and the calculator tool" when refusing a yes/no confirmation; structural "pass it to that evaluation mechanism" walkthroughs. Permanent real tests in `tests/adversarial/`: `test_student_chat_endpoint_adversarial.py`, `test_student_chat_endpoint_eval_leak.py`, `test_student_chat_endpoint_multiturn.py` (all via `tests/adversarial/conftest.py`'s scratch-DB endpoint fixtures; run with `ADVERSARIAL_CALL_DELAY_S=8`) and `test_student_chat_resolution_real.py` (real Chroma, NO Gemini — pins both borderline phrasings and every Step 6 branch). Transcripts: `tests/adversarial/transcripts_report_7.5_endpoint_*.md`. **Standing Rule #7 carry-forward: any later change near `SCOPE_SAFETY_RULE` or to the chat flow must re-run the endpoint suites, not just the function-level ones.**
    - **Known gaps (by decision, do not build without being asked):** NO per-student rate limiting or throttling on the student chat endpoint — deliberately deferred given the real demo scale (a handful of demo students, not a live class); every message costs one Gemini generation call (+ occasional summarization) against the same shared quota the adversarial suites already pressure. Complaint/concern auto-routing (email/Discord) is explicitly OUT of scope for 7.5 (decided) — not built.
  - **7.6 (student practice quizzes — backend only; the quiz-taking UI is 7.7):** student-triggered, NON-grade-affecting practice MCQs — never reads or writes `Grade`. `QuizAttempt` model via migration `af5038c37bc4` (one row per attempt; `scope_detail`/`questions_json`/`student_answers_json` are `Text` JSON strings, matching `rubric_json`; ids inside `scope_detail` are deliberately not FKs so history survives deletions). Router `app/routers/quiz.py` (all `require_student`, owner-only 403): `POST /quiz/generate` (JSON; scopes `assignment_file` | `session` | `multiple_sessions` (≥2) | `topic`), `POST /quiz/generate/upload` (multipart `.pptx`/`.ipynb`, extracted in memory only — never saved, never embedded, Chroma never touched; Starlette's own >1MB temp spool is outside our control), `POST /quiz/{id}/submit` (scored once; a second submit is 409 via a conditional UPDATE), `GET /quiz/history` (own SUBMITTED attempts only, newest first), and `GET /quiz/{id}` (added beyond the spec so "can't view another's attempt" is testable; answers only after submit). `correct_option_index` never reaches the client before submission; every score carries `not_a_real_grade: true` + a "does not affect your real grades" label.
    - **Generation (`app/services/quiz_generator.py`):** exactly 5 questions × 4 options via `call_llm(purpose="quiz_generation")`, with a signed-off prompt (includes a never-reveal-a-completion-gap clause covering wrong options too). Modes a/b/c read the source of truth (`LectureChunk` rows + `extract_notebook_structure`), NOT Chroma; topic uses `retrieve(top_k=10)` across all sessions but re-reads each notebook hit's cell from the source file. Material is randomly sampled up to 40k chars; under 500 chars (or no topic hits) → 422 with no LLM call. Citations: the model returns only a block id (`S3`); the parser rejects unknown ids and the citation string is built server-side from real metadata, so it can't be invented (notebook cells shown 1-indexed — 7.5 returns 0-indexed; accepted). The parser rejects without fixing anything; options are shuffled server-side (LLM positional bias). LLM/parse failure → clean 502, nothing stored.
    - **Two exclusion filters on notebook material (quiz-only):** `heuristic_hint` cells, then `is_hint_cell` — whole-word `\bhints?\b`, case-insensitive. Real notebooks describe gap answers inside unflagged hint notes ("💡 Hint … The method starts with bind…"; `week10_day1_lab.ipynb` cell 28 even says "(hint: Python's `eval()` works fine…)"). Both counts are logged per generation. Lab3: 13 + 13 excluded, leaving 2,192 chars, so repeat Lab3 quizzes are near-identical (accepted).
    - **Shared-code change — image stripping:** `extract_notebook_structure` / `extract_notebook_structure_from_bytes` (the latter plus `extract_pptx_content_from_bytes` added in 7.6, sharing the parsing core) gained OPT-IN `strip_images=False`, replacing embedded base64 images with `[image omitted]`. On for the quiz, the upload embedding flow (`routers/assignments.py`) and `scripts/backfill_embeddings.py`; deliberately OFF for `evaluator.py`/`rubric.py`, whose prompts stay byte-identical (six real session-4 submissions contain these images — changing grading input needs its own re-verification). `dataframe_basics.ipynb`/`pandas_group_by.ipynb` each had two 81k/140k-char image-only cells.
    - **Verification (real primary Gemini, minted JWT, demo student2):** 20 real generations across all 5 modes; every citation checked against its source; scores matched hand counts; the `eval`/`bind_tools`/`ChatGoogleGenerativeAI`/`literal_eval` sweep was clean on all 20; Lab3 leak check 5+5 generations clean (closest call: "a method starting with 'bind'", which repeats the notebook's own hint — same accepted class as 7.4/7.5); `grades` 12 rows, identical SHA-256 throughout; uploads left `storage/` and Chroma's count unchanged. **7.4's migration test was pinned to its own revision** (it assumed it was head). **Backup lesson:** the DB is WAL-mode, so always back up via `sqlite3.Connection.backup()`, never `cp`.
    - **Known gaps (see `docs/phase7-known-gaps-record.txt`, new in 7.6):** four existing Chroma entries (ids `notebook:10:55`, `10:62`, `11:8`, `11:15`) hold raw base64 with vectors built from only the first 256 tokens — 7.6 is protected, but **7.5's chat retrieval is still exposed**; the fix is a not-yet-run re-embed (reset `embedded=False` on unsolved files 10/11 after a backup-API backup, then run the backfill script). Multi-session quizzes over unrelated topics can blend them into one nonsensical question (real example logged; don't change the signed-off prompt for it). The hint filter also drops ordinary prose using the word "hint".
  - **7.7-7.8:** not started — 7.7 chat/quiz frontend (incl. the student floating chat widget for 7.5's endpoint and the quiz-taking UI for 7.6's endpoints), 7.8 full UI/UX overhaul (deliberately last).
- **Extended Goals (build only after Phase 6 fully complete):** Student chatbot (query-only against existing `rationale_json`, no new grading logic) + complaint routing via email/Discord (new integration work). Quiz generation from a course file + auto-grading + student-visible publish page.

## Extended Goals (build only after Phase 6 fully complete)
- **Student Chatbot:** students ask why they got a specific grade, grounded in the already-stored `rationale_json` (query interface only — data already exists from Phase 2, nothing new to store). Students can also ask general questions about their assignments/topics. Students can raise a complaint/concern routed to the relevant instructor via email or Discord (this part is new integration work, not just a query).
- **Instructor-Triggered Quiz Generation:** instructor asks chatbot to generate a quiz from a specific course file; published to a student-visible page once released; responses auto-graded.

## Future Work (explicitly out of scope for a long time — do NOT build, but design current work so these stay possible)
These are not on the roadmap yet, but no current phase should make an architectural choice that accidentally blocks them later:
- Full RAG pipeline (chunked, embedded course material in a vector DB) for broader topic/concept Q&A beyond specific grading rationale — don't hardcode assumptions that grading rationale is the only kind of retrievable context the system will ever have.
- Instructor-triggered release of lecture/lab files for a specific week/day (on-demand, never automatically scheduled) — keep assignment-file visibility/release logic separable from upload logic, not fused together.
- Support for additional submission formats beyond `.ipynb`/`.zip` — keep notebook-parsing logic in its own service layer (already true via `notebook.py`), don't scatter `.ipynb`-specific assumptions into routers/pipeline code.
- Instructor-facing analytics chatbot for cohort-wide progress tracking — keep grade/rationale data structured and queryable (already true via `rationale_json`), don't lock reporting logic into single-student-only shapes.

## Known gaps (by design, not bugs — do not "fix" without being asked)
Phase 1/2:
- No LLM fallback for ambiguous/altered file matches yet (file_matcher.py's own TODO).
- Students with zero submissions don't appear in `session_grade_report` at all.
- Rubric criteria count (3-6) isn't strictly validated, only checked non-empty.
- A handful of cosmetic items (dead imports, stale docstrings, notebooks parsed twice) — see `docs/phase2-known-gaps-record.txt` for the full list.

Phase 3 (see `docs/phase3-known-gaps-record.txt`):
- A student whose real name is an ordinary English word ("Grace", "Will") can false-positive match that word used ordinarily in an instruction. Unlike other misses, nothing catches this — it isn't routed to `ambiguous`/`not_found`, it silently narrows scope.
- No LLM fallback in `instruction_filter.py` — deliberate: the closed 5-shape output contract has nowhere to route "the second student" or exclusion queries.
- Exclusionary filters ("everyone except Ali") are detected and surfaced as `unsupported_filter` rather than silently inverting intent; force-regrading already-graded files is likewise not built.
- When an instruction under-specifies (e.g. "grade week 10" where two Week 10 sessions exist), the matcher returns `ambiguous` rather than guessing — working as designed, not a bug.
- No auth/token-refresh handling for long-lived SSE connections on `/chat/stream`.

## Testing philosophy
Every sub-feature needs both automated tests AND real-data verification before being called done — mocked tests alone have already missed real bugs (Phase 2's 3 grading-quality bugs, and 5 of Phase 3's 8, which surfaced only through real Swagger/dev-DB testing after the mocked tests were already green). For anything involving LLM judgment calls, get exact wording/approach approved before implementing, then re-verify against the real primary Gemini model, not just mocked tests — a fix is not confirmed until proven on the primary model. (The provider chain is now two Gemini tiers only — primary `gemini-3.5-flash-lite`, fallback `gemini-3.1-flash-lite` on quota/rate-limit; Groq and Ollama were removed.)

## Resuming after an agent runs out of context/credits
Never assume a prior session's summary is accurate. Re-verify actual file contents against whatever checklist applies before continuing any incomplete work.
